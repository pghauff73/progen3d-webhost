rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Firestore access is server-mediated in this app. The PHP Admin SDK bypasses rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
